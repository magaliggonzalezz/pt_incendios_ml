export class Hotspot {
  constructor({ fecha, ubicación, coordinates, temperatura, fuente }) {
    this.nombre = nombre;
    this.ubicación = ubicación;
    this.coordinates = coordinates;
     this.temperatura = temperatura;
    this.fecha = fecha;
  }
}